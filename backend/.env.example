# Environment Configuration Template
# Copy this file to .env and update with your actual values

# Server Configuration
NODE_ENV=production
PORT=4000

# Database
# IMPORTANT: Update with your MongoDB Atlas connection string
# Get from: https://cloud.mongodb.com → Connect → Connect your application
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/applier

# JWT Tokens (MUST CHANGE IN PRODUCTION)
# Generate secure random strings:
# Run in terminal: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Generate TWO different secrets (one for access, one for refresh)
JWT_SECRET=CHANGE_ME_GENERATE_RANDOM_32_CHAR_STRING
JWT_REFRESH_SECRET=CHANGE_ME_GENERATE_DIFFERENT_RANDOM_32_CHAR_STRING

# Token Expiry
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# CORS Configuration
# Add your Chrome Extension ID after publishing to Chrome Web Store
# Add your website domain if you build a web frontend
# Format: chrome-extension://YOUR_EXTENSION_ID,https://yourdomain.com
# Multiple origins separated by commas (no spaces)
ALLOWED_ORIGINS=http://localhost:3000,chrome-extension://YOUR_EXTENSION_ID_HERE

# Optional: OpenAI API (not currently used - deterministic AI only)
# If you want to add GPT features in the future, add key here
# OPENAI_API_KEY=sk-proj-your_key_here
